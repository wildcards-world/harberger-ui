/* TypeScript file generated from ProfileIcon.re by genType. */
/* eslint-disable import/first */


import * as React from 'react';

// tslint:disable-next-line:no-var-requires
const ProfileIconBS = require('./ProfileIcon.bs');

// tslint:disable-next-line:interface-over-type-literal
export type Props = { readonly clickAction?: () => void; readonly isMobile?: boolean };

export const make: React.ComponentType<{ readonly clickAction?: () => void; readonly isMobile?: boolean }> = ProfileIconBS.make;
